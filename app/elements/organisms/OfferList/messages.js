/*
 * OfferList Messages
 *
 * This contains all the text for the OfferList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.elements.OfferList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OfferList component!',
  },
});
