/*
 * TaskSwitch Messages
 *
 * This contains all the text for the TaskSwitch component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TaskSwitch';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TaskSwitch component!',
  },
});
