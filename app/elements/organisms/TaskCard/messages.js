/*
 * TaskCard Messages
 *
 * This contains all the text for the TaskCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.elements.TaskCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TaskCard component!',
  },
});
