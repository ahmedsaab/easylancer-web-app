// import { selectOfferDetailsModalDomain } from '../selectors';

describe('selectOfferDetailsModalDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
