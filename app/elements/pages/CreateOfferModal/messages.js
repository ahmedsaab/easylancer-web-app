/*
 * CreateOfferModal Messages
 *
 * This contains all the text for the CreateOfferModal container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CreateOfferModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CreateOfferModal container!',
  },
});
