/*
 * CreateTaskModal Messages
 *
 * This contains all the text for the CreateTaskModal container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CreateTaskModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CreateTaskModal container!',
  },
});
