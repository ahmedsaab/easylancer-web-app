/*
 * OfferDetailsModal Messages
 *
 * This contains all the text for the OfferDetailsModal container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OfferDetailsModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OfferDetailsModal container!',
  },
});
