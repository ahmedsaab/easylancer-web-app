/*
 * TaskHeader Messages
 *
 * This contains all the text for the TaskHeader component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.elements.TaskHeader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TaskHeader component!',
  },
});
