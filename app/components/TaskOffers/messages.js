/*
 * TaskOffers Messages
 *
 * This contains all the text for the TaskOffers component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TaskOffers';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TaskOffers component!',
  },
});
